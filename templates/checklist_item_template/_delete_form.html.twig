<form method="post" action="{{ path('app_checklist_item_template_delete', {'id': checklist_item_template.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ checklist_item_template.id) }}">
    <button class="btn">Delete</button>
</form>
